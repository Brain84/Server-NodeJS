# Server NodeJS + Express

* In this repository, you can find how to create a server using NodeJS and Express

* Click app.js file to see a code and its comment. The code presents several examples using `get` method like create a path to Home Page or a photo, redirect to another website, create a path to json, create and clear cookie

* Run the server with the following command `$ node app.js`